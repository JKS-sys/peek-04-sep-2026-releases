# Peek v1.0.9

_Released 2026-09-14_

Maintenance release. See the commit history for the full detail.

---

## Download

| Platform | File |
|---|---|
| macOS (Apple Silicon) | `Peek-1.0.9-arm64.dmg` |
| macOS (Intel) | `Peek-1.0.9.dmg` |
| Windows | `Peek-1.0.9-win.zip` — extract, then run `Peek Setup 1.0.9.exe` |
| Linux (AppImage) | `Peek-1.0.9.AppImage` |
| Linux (Debian/Ubuntu) | `peek_1.0.9_amd64.deb` |

Already running Peek? It updates itself — **Update → Check for Updates**, then
press **Install & Restart** when the download finishes.

## macOS: first launch

Peek is not notarised through the App Store, so Gatekeeper blocks it once:

```bash
xattr -cr /Applications/Peek.app
```

Then open it normally.

---

Peek is built by [Jagadeesh Kumar S](https://www.youtube.com/@JKS-sys) — NewsCraft Studio.
Report a problem: https://github.com/JKS-sys/peek-04-sep-2026-releases/issues
